logging:
  level:
    root: INFO
spring:
  application:
    name: sampleconfigclient
  cloud:
    config:
      uri: http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: "*"